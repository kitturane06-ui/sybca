wireless sensor node used for street light control

Algorithm :

Step1: Start initialize
Step 2: Read sensor value.
Step 3: Display Value
Step 4: Check light condition.
Steps: Repeat Continuously.
Step 6: END.

Code :

const int drpin = A0; //ldr pin
const int ledPin = 13;//led pin
int Idr Value = 0; // variable to store
LDR value
void setup() {
Serial.begin (9600);
pin Mode ledPin, OUTPUT);
pinMode (IdrPin, INPUT);
}
void loop(){
Idrvalue = analogRead (IdrPin); //
Read LDR value (0-1023)
Serial.print In (IdrValue); // Print to Serial Monitor
// If dark (value low), turn on LED
if (Idr Value <300){
digitalWrite (ledPin, HIGH);
}else {
digitalWrite(ledPin, Low);
}
delay (100);
}